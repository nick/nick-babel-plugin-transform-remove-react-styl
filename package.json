{
  "name": "babel-plugin-transform-remove-react-styl",
  "version": "1.0.4",
  "description": "",
  "repository": "nick/nick-babel-plugin-transform-remove-react-styl",
  "license": "MIT",
  "main": "lib/index.js",
  "devDependencies": {
    "babel": "^6.3.26",
    "babel-cli": "^6.4.5"
  },
  "scripts": {
    "build": "babel --presets es2015 src --out-dir lib --copy-files",
    "clean": "rm -rf lib",
    "prepublish": "npm run clean && npm run build"
  },
  "keywords": [
    "babel-plugin"
  ],
  "babel": {
    "presets": ["es2015"]
  }
}
